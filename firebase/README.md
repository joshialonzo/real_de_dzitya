# Firebase Environment

This directory will contain the Firebase initialization files (`firebase.json`, `.firebaserc`), Firestore Security Rules, Indexes, and Cloud Functions.

## Running Emulators Locally

Once Firebase is initialized and emulators are configured, you can start the local development environment using:

```bash
firebase emulators:start
```

*This will capture local frontend requests, isolating development from the production database.*
